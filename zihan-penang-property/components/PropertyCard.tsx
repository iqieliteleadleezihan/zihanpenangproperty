'use client';
import Image from 'next/image';
import Link from 'next/link';
import {ArrowUpRight,Check} from 'lucide-react';
import {Property} from '@/data/properties';
export default function PropertyCard({p}:{p:Property}){return <article className="group overflow-hidden rounded-[28px] bg-white shadow-sm"><Link href={`/properties/${p.slug}`} className="relative block aspect-[4/3] overflow-hidden"><Image src={p.image} fill alt={p.name} className="object-cover transition duration-700 group-hover:scale-[1.03]"/><div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-black/55 to-transparent p-5 pt-16 text-white"><div className="text-xs text-white/65">{p.location}</div><div className="mt-1 flex items-end justify-between"><h3 className="text-2xl font-bold">{p.name}</h3><ArrowUpRight/></div></div></Link><div className="p-5"><div className="flex items-center justify-between gap-4"><div><div className="eyebrow text-ink/40">Starting from</div><div className="mt-1 text-xl font-black">{p.priceLabel}</div></div><div className="text-right text-sm text-ink/55">{p.builtUp}<br/>{p.bedrooms} Bedrooms</div></div><div className="mt-5 grid gap-2 border-t border-black/8 pt-4">{p.highlights.slice(0,3).map(h=><div key={h} className="flex items-center gap-2 text-sm text-ink/65"><Check size={15}/>{h}</div>)}</div><div className="mt-5 grid grid-cols-2 gap-2"><Link href={`/properties/${p.slug}`} className="rounded-full border border-ink/15 px-4 py-3 text-center text-sm font-bold">View Details</Link><a href={`https://wa.me/60126564155?text=${encodeURIComponent(`Hi ZiHan, I'm interested in ${p.name} at ${p.location}. Can you send me the latest price and available units?`)}`} className="rounded-full bg-ink px-4 py-3 text-center text-sm font-bold text-white">Check Units</a></div></div></article>}
