import {MetadataRoute} from 'next';import {properties} from '@/data/properties';import {insights} from '@/data/insights';
export default function sitemap():MetadataRoute.Sitemap{const base='https://zihanproperty.com';return [{url:base,lastModified:new Date()},{url:`${base}/properties`,lastModified:new Date()},{url:`${base}/find`,lastModified:new Date()},{url:`${base}/compare`,lastModified:new Date()},{url:`${base}/calculator`,lastModified:new Date()},{url:`${base}/insights`,lastModified:new Date()},{url:`${base}/about`,lastModified:new Date()},{url:`${base}/contact`,lastModified:new Date()},...properties.map(p=>({url:`${base}/properties/${p.slug}`,lastModified:new Date()})),...insights.map(i=>({url:`${base}/insights/${i.slug}`,lastModified:new Date()}))]}
